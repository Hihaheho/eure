// Test: a key the schema does not know falls back to the path

schema = ```eure
name = `text`
```

editor = ```eure
nick|_|name = "Al"
```

scenarios_expected = ["hover"]

hover = ````markdown
**Error**

Missing required field 'name' at path (root)

---

**Error**

Unknown field 'nickname' at path (root)

---

```eure
nickname
```
````
