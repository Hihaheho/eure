// Explicit empty definition expectation; must not enable completions.

schema = ```eure
name = `text`
```

editor = ```eure
name = "Ada"
|_|
```

scenarios_expected = ["definition"]
definitions = []
