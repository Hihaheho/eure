// An import string opens the referenced file at its start.

files."common.eure" = ```eure
@ $types.user {
  name = `text`
}
```

editor = ```eure
$import = { common => "comm|_|on.eure" }
```

scenarios_expected = ["definition"]

@ definitions[]
file = "common.eure"
target = ```eure
|_|@ $types.user {
  name = `text`
}
```
