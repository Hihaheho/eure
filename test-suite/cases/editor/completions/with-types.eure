unimplemented = true

// Test: completion with types
// From: crates/eure-editor-support/tests/completions_test.rs:test_completion_with_types
// Schema with $types definitions

schema = ```eure
$types.Person {
    @ name
    $type = .text

    @ age
    $type = .number
}

@ user
$type = .$types.Person
```

editor = ```eure
@|_|
```

trigger = "@"

@ completions[]
label = "user"
kind = "field"

@ completions[]
label = "$types"
kind = "field"
