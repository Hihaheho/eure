// Test: completion right after the section marker
// From: crates/eure-editor-support/tests/completions_test.rs:test_completion_after_at_symbol

schema = ```eure
name = `text`
age = `integer`
active = `boolean`
```

editor = ```eure
@|_|
```

trigger = "@"

@ completions[]
label = "name"
kind = "field"

@ completions[]
label = "age"
kind = "field"

@ completions[]
label = "active"
kind = "field"
