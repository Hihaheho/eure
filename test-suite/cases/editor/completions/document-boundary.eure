unimplemented = true

// Test: completion at document boundary
// From: crates/eure-editor-support/tests/completions_test.rs:test_completion_at_document_boundary
// Completion at the very beginning or end of document

schema = ```eure
@ config
$type = .text

@ version
$type = .number
```

editor = ```eure
|_|
```

// Should provide root-level completions for empty document
// TODO: Add specific expected completions when implementing
@ completions[]
label = "config"

@ completions[]
label = "version"
