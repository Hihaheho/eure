unimplemented = true

# Test: array element completion
# From: crates/eure-editor-support/tests/completions_test.rs:test_array_element_completion
# Completion for array elements like items[].

schema = ```eure
$types.Item {
    @ id
    $type = .number

    @ name
    $type = .text

    @ description
    $type = .text
    $optional = true

    @ price
    $type = .number
}

@ items {
    $array = .$types.Item
}
```

editor = ```eure
@ items[]
|_|
```

# Should have array element fields: id, name, description, price

@ completions[]
label = "id"
kind = "field"

@ completions[]
label = "name"
kind = "field"

@ completions[]
label = "description"
kind = "field"

@ completions[]
label = "price"
kind = "field"
