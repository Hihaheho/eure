unimplemented = true

// Test: enum value completion
// From: crates/eure-editor-support/tests/completions_test.rs:test_enum_value_completion
// Completion of enum values for fields with $enum constraint
// TODO: enum completions not yet implemented

schema = ```eure
@ config {
    @ environment {
        $type = .text
        $enum = ["development", "staging", "production"]
    }

    @ log_level {
        $type = .text
        $enum = ["debug", "info", "warn", "error", "fatal"]
    }

    @ port {
        $type = .number
    }
}
```

editor = ```eure
@ config
environment = |_|
```

trigger = "="

@ completions[]
label = "\"development\""
kind = "value"

@ completions[]
label = "\"staging\""
kind = "value"

@ completions[]
label = "\"production\""
kind = "value"
