unimplemented = true

# Test: schema manager
# From: crates/eure-editor-support/tests/schema_validation_test.rs:test_schema_manager
# Tests loading schemas and associating documents with schemas

schema = ```eure
@ $types.Person {
    name.$type = .text
    age.$type = .number
}
```

editor = ```eure
person.$type = .$types.Person

person = {
    name = "Alice"
    age = 25
}
```

# Schema manager should:
# 1. Successfully load the schema
# 2. Associate document with schema URI
# 3. Make schema available via get_schema()
schema_uri = "test://person.schema.eure"
document_uri = "test://doc.eure"
