// Valid: $description is optional, so it can be omitted

input_eure = ```eure
name = "Alice"
```

schema = ```eure
name = `text`
name.$ext-type.description = `text`
name.$ext-type.description.$optional = true
```
