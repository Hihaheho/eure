input_eure = ```eure
@ empty-section

@ empty-block {}

@ empty-values
map = {}
array = []
tuple = ()
```

normalized = ```eure
= {
	empty-section => {}
	empty-block => {}
  empty-values => { map => {}, array => [], tuple => () }
}
```

output_json = ```json
{
	"empty-section": {},
	"empty-block": {},
	"empty-values": { "map": {}, "array": [], "tuple": [] }
}
```

@ cases.section-with-hole
input_eure = ```eure
@ section
= !
```

normalized = ```eure
= {
	section => !
}
```
