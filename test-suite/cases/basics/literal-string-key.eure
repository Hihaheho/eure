// Test: Literal string keys
// Single-quoted strings can be used as keys, allowing special characters without escaping

// Simple literal key
input_eure = ```eure
'#' = 1
```

normalized = ```eure
= { "#" => 1 }
```

json = json`{ "#": 1 }`

// Literal key with quotes inside (using delimited literal string)
@ cases.key-with-quotes
input_eure = ```eure
<'What's "query?"'> = "answer"
```

normalized = ```eure
= { "What's \"query?\"" => "answer" }
```

output_json = json`{ "What's \"query?\"": "answer" }`

// Multiple literal keys in path
@ cases.path-with-literal-keys
input_eure = ```eure
'#'.'Introduction' = 1
```

normalized = ```eure
= { "#" => { "Introduction" => 1 } }
```

json = json`{ "#": { "Introduction": 1 } }`

// Literal key in section (using delimited literal string for single quote)
@ cases.section-with-literal-key
input_eure = ```eure
@ '#'.<'What's this?'>
value = 42
```

normalized = ```eure
= { "#" => { "What's this?" => { value => 42 } } }
```

json = json`{ "#": { "What's this?": { "value": 42 } } }`

// Nested sections with literal keys
@ cases.nested-sections
input_eure = ```eure
@ '#' {
  @ '##' {
    content = "hello"
  }
}
```

normalized = ```eure
= { "#" => { "##" => { content => "hello" } } }
```

json = json`{ "#": { "##": { "content": "hello" } } }`

// Mixed regular and literal keys
@ cases.mixed-keys
input_eure = ```eure
regular = 1
'special key' = 2
"quoted" = 3
```

normalized = ```eure
= { regular => 1, "special key" => 2, "quoted" => 3 }
```

json = json`{ "regular": 1, "special key": 2, "quoted": 3 }`

// Literal key with spaces
@ cases.key-with-spaces
input_eure = ```eure
'key with spaces' = "value"
```

normalized = ```eure
= { "key with spaces" => "value" }
```

json = json`{ "key with spaces": "value" }`

// Empty literal key
@ cases.empty-key
input_eure = ```eure
'' = "empty key"
```

normalized = ```eure
= { "" => "empty key" }
```

json = json`{ "": "empty key" }`
