input_eure = ```eure
a = -1
b = -42
c = -1_000
d = [-1, -2, -3]
e = { x => -5, y => -10 }
```

normalized = ```eure
= {
  a => -1,
  b => -42,
  c => -1000,
  d => [-1, -2, -3],
  e => { x => -5, y => -10 },
}
```

output_json = ```json
{ "a": -1, "b": -42, "c": -1000, "d": [-1, -2, -3], "e": { "x": -5, "y": -10 } }
```
