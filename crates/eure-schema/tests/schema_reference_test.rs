//! Integration tests for $schema references and external schema validation
//! 
//! TODO: Update these tests to use the new value-based API
//! Currently commented out as they use the old CST-based API

// use eure_parol::parse;
// use eure_schema::{extract_schema, validate_self_describing, validate_with_schema};
// use std::fs;

// The tests in this file need to be rewritten to use the new value-based API
// which uses extract_schema_from_value and validate_with_schema_value functions